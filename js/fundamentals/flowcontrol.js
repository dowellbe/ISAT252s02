var age = 20  
// The current age is 20.
if (age < 18) { 
    text = "Not of legal voting age.";
// If current age is less than 18, 
// output is "Not of legal voting age."
} else if (age < 21) {
    text = "Not of legal drinking age.";
// If current age is less than 21, 
// output is "Not of legal drinking age."
} else {
    text = "Too young or too old.";
}
// Output is "Not of legal drinking age."